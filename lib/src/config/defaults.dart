/// Default Claude model used for AI-powered fixes.
const String defaultModel = 'claude-sonnet-4-6';

/// Default maximum tokens per AI response.
const int defaultMaxTokens = 1024;

/// Default directory for JSON audit reports.
const String defaultReportsDir = '.semantifix_reports';

/// Default directory for pre-patch file backups.
const String defaultBackupsDir = '.semantifix_backups';
